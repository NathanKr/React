class Options{
    static ALL='All';
    static ACTIVE ='Active';
    static COMPLETED = 'Completed';

    static options = [ Options.ALL, Options.ACTIVE,Options.COMPLETED];
}

export default Options;